# New version 1.0

Converting entire build to Automake and renaming several files forced restarting the repo from scratch with git `--mirror` from new repository for consistency.

We separated out all the functions into a separate library.